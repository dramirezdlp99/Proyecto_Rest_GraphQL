name: proyecto_rest_graphql
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Para REST
  http: ^1.1.0
  
  # Para GraphQL y Gestión de Estado
  graphql_flutter: ^5.1.2
  get: ^4.6.6
  connectivity_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true